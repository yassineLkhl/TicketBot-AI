{
  "name": "prisma-service",
  "private": true,
  "scripts": {
    "migrate:dev": "prisma migrate dev --schema=./schema.prisma",
    "migrate:reset": "prisma migrate reset --schema=./schema.prisma",
    "generate": "prisma generate --schema=./schema.prisma"
  },
  "devDependencies": {
    "prisma": "^6.13.0"
  },
  "dependencies": {
    "@prisma/client": "^6.13.0"
  }
}